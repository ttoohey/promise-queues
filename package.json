{
  "name": "@ttoohey/promise-queues",
  "version": "1.0.0",
  "description": "Throttle and debounce async tasks",
  "main": "lib/index.js",
  "files": [
    "lib/*.js"
  ],
  "scripts": {
    "build": "babel -d lib/ src/",
    "clean": "rm -rf lib/",
    "prepare": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "promise",
    "debounce",
    "throttle"
  ],
  "author": "Gency Digital <hello@gency.com.au>",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3",
    "babel-preset-minify": "^0.5.1"
  }
}
